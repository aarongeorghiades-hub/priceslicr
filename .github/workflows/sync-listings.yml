name: Sync eBay Listings

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger sync
        run: |
          curl -s --max-time 180 "https://www.priceslicr.com/api/sync-listings?secret=priceslicr-sync-2026"
